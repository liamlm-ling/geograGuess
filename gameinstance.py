class gameInstance:
    
    def __init__(self, location):
        self.location = location
        self.guess = None

    def calc_distance(loc):
        return 0
    
    def calc_score(dist):
        return 0

    def evaluate_guess(self, guess):
        dist = calc_distance(guess)
        score = calc_score(dist)